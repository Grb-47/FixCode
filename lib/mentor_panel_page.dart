import 'package:flutter/material.dart';

class MentorPanelPage extends StatelessWidget {
  const MentorPanelPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Mentor Panel")),
      body: Center(child: Text("Mentor Panel Page")),
    );
  }
}
